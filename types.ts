export interface Stage {
  id: string;
  name: string;
  position: number;
  // opportunityLayout: number[];
  // opportunities: Opportunity[];
}

export interface Opportunity {
  id: string;
  name: string;
}
